<!DOCTYPE html>
<html lang="en">

  <head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>THE #1 Tech Blog</title>
    
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.0-beta3/dist/css/bootstrap.min.css" rel="stylesheet"
      integrity="sha384-eOJMYsd53ii+scO/bJGFsiCZc+5NDVN2yr8+0RDqr0Ql0h+rP48ckxlpbzKgwra6" crossorigin="anonymous">
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.0.0-beta3/dist/js/bootstrap.bundle.min.js" integrity="sha384-JEW9xMcG8R+pH31jmWH6WWP0WintQrMb4s7ZOdauHnUtxwoG2vI5DkLtS3qm9Ekf" crossorigin="anonymous"></script>
    <link rel="stylesheet" href="https://fonts.googleapis.com/icon?family=Material+Icons">
    <link rel="stylesheet" href="/css/style.css">
  </head>

  <body>
    <header>
      {{#if logged_in}}
        <nav class="navbar navbar navbar-dark" style="background-color: #3F72AF">
      {{else}}
        <nav class="navbar navbar navbar-dark" style="background-color: #3F72AF">
      {{/if}}
      <div class="container">
        <div class="navbar navbar-inner">
          <a href="/"><i class="material-icons custom ga_icons" id="home">public</i></a>
          {{!-- <a class="nav-link" id="home" aria-current="page" href="/">TechBlog</a> --}}
          {{#if logged_in}}
            <a href="/dashboard"><i class="material-icons custom ga_icons" id="dashboard">perm_identity</i></a>
            <a href=""><i class="material-icons custom ga_icons" id="logout">logout</i></a>
          {{else}}
            <a href="/login"><i class="material-icons custom ga_icons" id="login">login</i></a>
          {{/if}}
          
        </div>
        <a class="navbar-brand ga_icons" style="float:right; color:#083C5A" href="/">Tech Blog</a>
      </div>
      </nav>
    </header>

    <main class="container container-fluid mt-5">
      <!-- Render the sub layout -->
      {{{ body }}}
    </main>

    <footer class="footer">
      <!-- Copyright -->
      <div class="text-muted" style="text-align: center; margin-top: 200px">
        2021&#169; CDC 
      </div>
    </footer>

    <!-- Render script for logged in users only -->
    {{#if logged_in}}
      <script src="/js/logout.js"></script>
    {{/if}}
  </body>

</html>